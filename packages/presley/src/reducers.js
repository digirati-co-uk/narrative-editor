import { reducer as metadata } from './spaces/metadata';
import { reducer as annotations } from './spaces/annotations';
import { reducer as tileSource } from './spaces/tileSource';
import { reducer as canvas } from './spaces/canvas';

export default { metadata, annotations, tileSource, canvas };
